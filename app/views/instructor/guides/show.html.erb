<%= link_to 'Preview as Student', guide_path(@guide), class: 'btn btn-warning btn-lg stdnt-btn' %>
<br />
<div class="new-box mx-auto text-center">
	<h1><%= @guide.title %></h1>
	<h4><%= @guide.description %></h4>
 
</div> 



  <% @guide.sections.each do |section| %>
    <div class="new-box text-center mx-auto">
      <ul>
        <li>
          <h3><%= section.title %></h3>
        </li>
      </ul>
        <% section.lessons.each_with_index do |lesson, position| %>
        <ul>
          <li>
            <b>Lesson <%= position+1 %>: <%= lesson.title %></b>
            -
            <%= lesson.subtitle %>
          </li>
        </ul>
      <% end %>

      <br />
      <%= link_to "New Lesson", new_instructor_section_lesson_path(section), class: 'btn btn-primary' %>
    </div>
  <% end %>
<br />

<div class="text-center">
  <%= link_to "Add a new section", new_instructor_guide_section_path(@guide), class: 'btn btn-primary btn-lg' %>

</div>